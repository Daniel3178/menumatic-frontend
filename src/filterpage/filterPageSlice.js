import { createAsyncThunk, createSlice } from "@reduxjs/toolkit";

const filterPage = createSlice({
    name: "filterPage",
    initialState: {
        likesCounter: 0,
        showInfo: false,
    },
    reducers: {
        

    },
    });